#include <stdio.h>

int main() {
   int n, i, count = 0;

   printf("Enter the number of even numbers to print: ");
   scanf("%d", &n);

   for (i = 1; count < n; i++) {
      if (i % 2 == 0) {
         printf("%d ", i);
         count++;
      }
   }

   return 0;
}
